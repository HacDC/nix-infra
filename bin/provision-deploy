#!/usr/bin/env bash
set -euo pipefail

cd "$ROOT"
nix build --no-link .#deploy.nodes.factorio.profiles.system.path
nix build --no-link .#deploy.nodes.terraria.profiles.system.path
tofu apply
deploy --skip-checks .#factorio
deploy --skip-checks .#terraria
